#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - contains all the codes needed in my git.
 *
 * Return: Always 0.
 */
int main(void)
{
  char *concat;

  concat = string_nconcat("main ", NULL, 1);
  printf("%s\n", concat);
  free(concat);
  return (0);
}
